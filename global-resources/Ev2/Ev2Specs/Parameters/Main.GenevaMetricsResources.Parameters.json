// Definitions and purposes of each parameter detailed specified in the Main.GenevaResources.Template.bicep file.
{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "resourcesName": {
            "value": "{{.resourcesName}}"
        },
        "subscriptionId": {
            "value": "{{.subscriptionId}}"
        },
        "resourceGroupName": {
            "value": "{{.resourceGroupName}}"
        },
        "resourceGroupLocation": {
            "value": "{{.location}}"
        },
        "tenantId": {
            "value": "{{.tenantId}}"
        },
        "corpAdminSecurityGroupId": {
            "value": "{{.corpAdminSecurityGroupId}}"
        }
    }
}