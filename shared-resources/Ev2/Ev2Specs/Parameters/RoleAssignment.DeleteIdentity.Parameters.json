{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "subscriptionId": {
            "value": "{{.subscriptionId}}"
        },
        "location": {
            "value": "{{.location}}"
        },
        "globalSubscriptionId": {
            "value": "{{.globalSubscriptionId}}"
        },
        "globalResourcesName": {
            "value": "{{.globalResourcesName}}"
        }
    }
}