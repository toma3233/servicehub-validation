{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "resourcesName": {
            "value": "{{.resourcesName}}"
        },
        "globalResourcesName": {
            "value": "{{.globalResourcesName}}"
        },
        "resourceGroupName": {
            "value": "{{.resourceGroupName}}"
        },
        "subscriptionId": {
            "value": "{{.subscriptionId}}"
        },
        "globalSubscriptionId": {
            "value": "{{.globalSubscriptionId}}"
        },
        "aksClusterName": {
            "value": "__AKS_CLUSTER_NAME__"
        }
    }
}